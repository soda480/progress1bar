name: build
on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-20.04
    container: python:3.9-slim

    steps:
    - uses: actions/checkout@v2

    - name: Install pybuilder
      run: pip install pybuilder

    - name: Install dependencies
      run: pyb install_dependencies --no-venvs

    - name: Execute build
      run: pyb -X --no-venvs

    - name: Install Codecov requirements
      run: |
        apt-get update
        apt-get install -y bash curl
        sed -e 's,filename="progress1bar/,filename="src/main/python/progress1bar/,g' target/reports/progress1bar_coverage.xml > coverage.xml

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v1
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        file: coverage.xml